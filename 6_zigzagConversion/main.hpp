#ifndef MAIN
#define MAIN
#include <string>

class Solution{
    public:
    std::string convert(std::string s, int numRows);
    int overCol(int length, int numRows);
    int Row(int length, int numRows);
};

#endif